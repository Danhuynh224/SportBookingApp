<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" /> <!-- Màu nền -->
    <stroke
        android:width="1dp"
        android:color="@color/textDisabled" /> <!-- Màu và độ dày viền -->
    <corners android:radius="8dp" /> <!-- Bo góc -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>