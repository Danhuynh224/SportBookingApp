<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white"/> <!-- Màu nền của CardView -->
    <corners android:radius="16dp"/> <!-- Độ bo góc của CardView -->
    <stroke
        android:color="@color/textDisabled"
    android:width="1dp"/>
</shape>
